const GetAllAdvertisments = {
  query: (path) => ({
    url: path,
  }),
};
const GetSpecificAdvertisment = {
  query: (path) => ({
    url: path,
  }),
};

export const AdvetismentQuery = { GetAllAdvertisments, GetSpecificAdvertisment };
