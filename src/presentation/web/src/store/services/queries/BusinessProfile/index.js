const GetProfile = {
  query: (path) => ({
    url: path,
  }),
};
const GetSpecificProfile = {
  query: (path) => ({
    url: path,
  }),
};

export const ProfileQueries = { GetProfile, GetSpecificProfile };
